-- Your SQL goes here

CREATE TABLE rapid_entry (
    id INTEGER NOT NULL,
    fullname TEXT NOT NULL,
    hash TEXT NOT NULL,
    something TEXT NOT NULL,
    alias TEXT NOT NULL,
    PRIMARY KEY (id)
);
